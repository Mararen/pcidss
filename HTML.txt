Dentro de templates\users

login.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Login | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body class="login-page">

    <!-- HERO / BANNER -->
    <section class="login-hero">
        <img src="{% static 'users/img/banner.jpg' %}" alt="Banner">

        <div class="login-hero-overlay">
            <img src="{% static 'users/img/logo.png' %}" class="login-logo">
        </div>
    </section>

    <!-- LOGIN -->
    <section class="login-wrapper">

        <div class="login-card">

            <form method="post">
                {% csrf_token %}

                {% if error %}
                    <div class="login-error">{{ error }}</div>
                {% endif %}

                <label>Nombre de usuario</label>
                <input type="text" name="username" required>

                <label>Contraseña</label>
                <input type="password" name="password" required>

                <label class="remember">
                    <input type="checkbox" name="remember">
                    Recordarme
                </label>

                <button type="submit" class="btn-primary">
                    Iniciar Sesión
                </button>

                <div class="forgot">
                    <a href="{% url 'forgot_password' %}">¿Olvidaste tu contraseña?</a>
                </div>

            </form>

        </div>

    </section>

</body>
</html>

entidades_lista.html

{% load static %}

<!DOCTYPE html>

<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Entidades | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body>

<div class="layout">

<!-- SIDEBAR -->
<aside class="sidebar">
    <div class="profile">
        <img src="{% static 'users/img/profile.png' %}" alt="Perfil">
        <h4>{{ request.user.get_full_name|default:request.user.username }}</h4>
        <p>Administrador Global</p>
    </div>

    <nav class="menu">
        <a href="{% url 'dashboard' %}">
            <i data-feather="grid"></i>
            Dashboard
        </a>

        <a href="{% url 'usuarios_lista' %}">
            <i data-feather="users"></i>
            Usuarios
        </a>

        <a href="{% url 'entidades_lista' %}" class="active">
            <i data-feather="home"></i>
            Entidades
        </a>

        <a href="{% url 'saq' %}">
            <i data-feather="file-text"></i>
            SAQ
        </a>

        <a href="#">
            <i data-feather="check-square"></i>
            Pre-Test
        </a>

        <a href="#">
            <i data-feather="paperclip"></i>
            Evidencias
        </a>

        <a href="#">
            <i data-feather="star"></i>
            Renovación
        </a>

        <a href="{% url 'logout' %}" class="logout">
            <i data-feather="log-out"></i>
            Cerrar Sesión
        </a>
    </nav>
</aside>


<!-- MAIN -->
<main class="main-content">

    <!-- TOPBAR -->
    <header class="topbar">

        <nav class="topbar-nav">
            <a href="{% url 'dashboard' %}">Dashboard</a>
            <a href="#">Catálogos</a>
            <a href="#">Configuración</a>
            <a href="#">Ayuda</a>
        </nav>

        <div class="topbar-right">

            <div class="topbar-search">
                <i data-feather="search"></i>
                <input type="text" placeholder="Buscar...">
            </div>

            <div class="topbar-lang">
                Eng ▾
            </div>

        </div>

    </header>


    <!-- CONTENT -->
    <section class="content">

        <!-- PAGE HEADER -->
        <div class="page-header">

            <div class="page-header-text">
                <h1>Administración de entidades</h1>
                <p>
                    Crea, edita o elimina entidades, define roles con distintos niveles de acceso y gestiona la autenticación
                    de manera centralizada para mantener el control y la trazabilidad de acciones.
                </p>
            </div>

            <div class="page-header-logo-min">
                <img src="{% static 'users/img/logo.png' %}" alt="PCI Cert Pro">
            </div>

        </div>


        <!-- ADD BUTTON -->
        <div style="display:flex; justify-content:flex-end; margin-bottom:16px;">
            <a href="{% url 'entidad_crear' %}" class="btn-add" title="Nueva Entidad">
                <i data-feather="plus"></i>
            </a>
        </div>


        <!-- TABLE CONTROLS -->
        <div class="table-controls">

            <div class="table-controls-left">
                <label>Mostrar</label>

                <form method="get" style="display:flex;align-items:center;gap:8px;">
                    <input type="number"
                           name="per_page"
                           value="{{ request.GET.per_page|default:'5' }}"
                           min="5"
                           max="100"
                           style="width:60px;">
                    <label>registros</label>
                </form>
            </div>

            <div class="table-controls-right">
                <label>Buscar</label>

                <form method="get" class="search-box">
                    <input type="text"
                           name="buscar"
                           value="{{ request.GET.buscar }}">
                </form>
            </div>

        </div>


        <!-- TABLE -->
        <table class="modern-table">

            <thead>
                <tr>
                    <th>Usuario <span class="sort-icon">↑↓</span></th>
                    <th>Nombre de la empresa <span class="sort-icon">↑↓</span></th>
                    <th>DBA <span class="sort-icon">↑↓</span></th>
                    <th>Email <span class="sort-icon">↑↓</span></th>
                    <th>Última modificación <span class="sort-icon">↑↓</span></th>
                    <th>Contacto <span class="sort-icon">↑↓</span></th>
                    <th>Activo</th>
                    <th>Acciones</th>
                </tr>
            </thead>

            <tbody>

            {% for entidad in entidades %}

                <tr>

                    <td>{{ entidad.usuario.username }}</td>

                    <td>{{ entidad.nombre_empresa }}</td>

                    <td>{{ entidad.dba }}</td>

                    <td>{{ entidad.email }}</td>

                    <td style="color:var(--text-muted);">
                        {{ entidad.updated_at|default:"—" }}
                    </td>

                    <td>{{ entidad.contacto }}</td>

                    <td>

                        {% url 'entidad_toggle' entidad.pk as toggle_url %}

                        <label class="switch">

                            <input type="checkbox"
                                   {% if entidad.is_active %}checked{% endif %}
                                   onchange="location.href='{{ toggle_url }}'">

                            <span class="slider"></span>

                        </label>

                    </td>

                    <td>

                        <div class="actions">

                            <!-- VER -->
                            <a href="{% url 'entidad_detalle' entidad.pk %}"
                               class="btn-icon"
                               title="Ver">

                                <i data-feather="eye"></i>

                            </a>


                            <!-- EDITAR -->
                            <a href="{% url 'entidad_editar' entidad.pk %}"
                               class="btn-icon"
                               title="Editar">

                                <i data-feather="edit-2"></i>

                            </a>


                            <!-- DESACTIVAR -->
                            <a href="{% url 'entidad_toggle' entidad.pk %}"
                               class="btn-icon danger"
                               title="Desactivar">

                                <i data-feather="slash"></i>

                            </a>

                        </div>

                    </td>

                </tr>

            {% empty %}

                <tr>
                    <td colspan="8"
                        style="text-align:center;
                               color:var(--text-muted);
                               padding:30px;">

                        No hay entidades registradas.

                    </td>
                </tr>

            {% endfor %}

            </tbody>

        </table>


        <!-- PAGINATION -->
        <div class="pagination">

            <div class="pagination-info">

                Mostrando registros del {{ page_obj.start_index }}
                al {{ page_obj.end_index }}

                de un total de {{ page_obj.paginator.count }} registros

                (filtrado de un total de
                {{ total_registros|default:page_obj.paginator.count }}
                registros)

            </div>


            <div class="pagination-controls">

                {% if page_obj.has_previous %}
                    <a href="?page={{ page_obj.previous_page_number }}&buscar={{ request.GET.buscar }}">
                        Anterior
                    </a>
                {% else %}
                    <span style="opacity:0.4;">Anterior</span>
                {% endif %}

                {% for num in page_obj.paginator.page_range %}

                    {% if num == page_obj.number %}

                        <span class="current">{{ num }}</span>

                    {% elif num|add:"-1" == page_obj.number or num|add:"1" == page_obj.number %}

                        <a href="?page={{ num }}&buscar={{ request.GET.buscar }}">
                            {{ num }}
                        </a>

                    {% endif %}

                {% endfor %}

                {% if page_obj.has_next %}
                    <a href="?page={{ page_obj.next_page_number }}&buscar={{ request.GET.buscar }}">
                        Siguiente
                    </a>
                {% else %}
                    <span style="opacity:0.4;">Siguiente</span>
                {% endif %}

            </div>

        </div>

    </section>

</main>
```

</div>

<script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>

<script>
    feather.replace({ 'stroke-width': 2 })
</script>

</body>
</html>

forgot_password.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Recuperar Contraseña | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body class="login-page">

    <section class="login-hero">
        <img src="{% static 'users/img/banner.jpg' %}" alt="Banner">
        <div class="login-hero-overlay">
            <img src="{% static 'users/img/logo.png' %}" class="login-logo">
        </div>
    </section>

    <section class="login-wrapper">
        <div class="login-card">

            <h2 style="font-size:17px; margin-bottom:4px;">Recuperar contraseña</h2>
            <p style="font-size:13px; color:var(--text-muted); margin-bottom:8px;">
                Ingresa tu correo y te enviaremos un enlace para restablecer tu contraseña.
            </p>

            {% if message %}
                <div class="login-error" style="color:var(--success); background:var(--success-bg);">
                    {{ message }}
                </div>
            {% endif %}

            {% if error %}
                <div class="login-error">{{ error }}</div>
            {% endif %}

            <form method="post">
                {% csrf_token %}
                <label>Correo electrónico</label>
                <input type="email" name="email" required placeholder="tucorreo@empresa.com">
                <button type="submit" class="btn-primary" style="width:100%; margin-top:14px;">
                    Enviar enlace
                </button>
            </form>

            <div class="forgot" style="margin-top:14px;">
                <a href="{% url 'login' %}">← Volver al inicio de sesión</a>
            </div>

        </div>
    </section>

</body>
</html>

reset_password_confirm.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Nueva Contraseña | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body class="login-page">

    <section class="login-hero">
        <img src="{% static 'users/img/banner.jpg' %}" alt="Banner">
        <div class="login-hero-overlay">
            <img src="{% static 'users/img/logo.png' %}" class="login-logo">
        </div>
    </section>

    <section class="login-wrapper">
        <div class="login-card">

            {% if validlink %}
                <h2 style="font-size:17px; margin-bottom:4px;">Nueva contraseña</h2>
                <p style="font-size:13px; color:var(--text-muted); margin-bottom:8px;">
                    Ingresa tu nueva contraseña dos veces para confirmarla.
                </p>
                <form method="post">
                    {% csrf_token %}
                    {% for field in form %}
                        <label>{{ field.label }}</label>
                        {{ field }}
                        {% if field.errors %}
                            <div class="login-error">{{ field.errors }}</div>
                        {% endif %}
                    {% endfor %}
                    <button type="submit" class="btn-primary" style="width:100%; margin-top:14px;">
                        Guardar contraseña
                    </button>
                </form>

            {% else %}
                <h2 style="font-size:17px; margin-bottom:8px;">Enlace inválido</h2>
                <p style="font-size:13px; color:var(--text-muted);">
                    Este enlace ya fue usado o ha expirado. Solicita uno nuevo.
                </p>
                <div style="margin-top:14px;">
                    <a href="{% url 'forgot_password' %}" class="btn-primary"
                       style="display:block; width:100%; text-align:center; text-decoration:none; padding:10px;">
                        Solicitar nuevo enlace
                    </a>
                </div>
            {% endif %}

            <div class="forgot" style="margin-top:14px;">
                <a href="{% url 'login' %}">← Volver al inicio de sesión</a>
            </div>

        </div>
    </section>

</body>
</html>

reset_password.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Nueva Contraseña | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body class="login-page">

    <section class="login-hero">
        <img src="{% static 'users/img/banner.jpg' %}" alt="Banner">
        <div class="login-hero-overlay">
            <img src="{% static 'users/img/logo.png' %}" class="login-logo">
        </div>
    </section>

    <section class="login-wrapper">
        <div class="login-card">

            {% if validlink %}
                <h2 style="font-size:17px; margin-bottom:4px;">Nueva contraseña</h2>
                <p style="font-size:13px; color:var(--text-muted); margin-bottom:8px;">
                    Ingresa tu nueva contraseña dos veces para confirmarla.
                </p>

                <form method="post">
                    {% csrf_token %}
                    {% for field in form %}
                        <label>{{ field.label }}</label>
                        {{ field }}
                        {% if field.errors %}
                            <div class="login-error">{{ field.errors }}</div>
                        {% endif %}
                    {% endfor %}
                    <button type="submit" class="btn-primary" style="width:100%; margin-top:14px;">
                        Guardar contraseña
                    </button>
                </form>

            {% else %}
                <h2 style="font-size:17px; margin-bottom:8px;">Enlace inválido</h2>
                <p style="font-size:13px; color:var(--text-muted);">
                    Este enlace ya fue usado o ha expirado. Solicita uno nuevo.
                </p>
                <div style="margin-top:14px;">
                    <a href="{% url 'forgot_password' %}" class="btn-primary"
                       style="display:block; width:100%; text-align:center; text-decoration:none; padding:10px;">
                        Solicitar nuevo enlace
                    </a>
                </div>
            {% endif %}

            <div class="forgot" style="margin-top:14px;">
                <a href="{% url 'login' %}">← Volver al inicio de sesión</a>
            </div>

        </div>
    </section>

</body>
</html>

usuarios_eliminar.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Eliminar Usuario | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body>

<div class="layout">

    <!-- SIDEBAR -->
    <aside class="sidebar">
        <div class="profile">
            <img src="{% static 'users/img/profile.png' %}" alt="Perfil">
            <h4>{{ request.user.get_full_name|default:request.user.username }}</h4>
            <p>Administrador Global</p>
        </div>
        <nav class="menu">
            <a href="{% url 'dashboard' %}">
                <i data-feather="grid"></i>
                Dashboard
            </a>
            <a href="{% url 'usuarios_lista' %}" class="active">
                <i data-feather="users"></i>
                Usuarios
            </a>
            <a href="{% url 'entidades_lista' %}">
                <i data-feather="home"></i>
                Entidades
            </a>
            <a href="{% url 'saq' %}">
                <i data-feather="file-text"></i>
                SAQ
            </a>
            <a href="#">
                <i data-feather="check-square"></i>
                Pre-Test
            </a>
            <a href="#">
                <i data-feather="paperclip"></i>
                Evidencias
            </a>
            <a href="#">
                <i data-feather="star"></i>
                Renovación
            </a>
            <a href="{% url 'logout' %}" class="logout">
                <i data-feather="log-out"></i>
                Cerrar Sesión
            </a>
        </nav>
    </aside>

    <!-- MAIN -->
    <main class="main-content">

    <!-- TOPBAR -->
    <header class="topbar">

        <nav class="topbar-nav">
            <a href="{% url 'dashboard' %}">Dashboard</a>
            <a href="#">Catálogos</a>
            <a href="#">Configuración</a>
            <a href="#">Ayuda</a>
        </nav>

        <div class="topbar-right">

            <div class="topbar-search">
                <i data-feather="search"></i>
                <input type="text" placeholder="Buscar...">
            </div>

            <div class="topbar-lang">
                Eng ▾
            </div>

        </div>

    </header>

        <!-- CONTENT -->
        <section class="content">

            <!-- PAGE HEADER -->
            <div class="page-header">
                <div class="page-header-text">
                    <h1>Eliminar Usuario</h1>
                    <p>Esta acción es permanente y no se puede deshacer.</p>
                </div>
                <div class="page-header-pcilogo-min">
                    <img src="{% static 'users/img/pcilogo.png' %}" alt="PCI Cert Pro">
                </div>
            </div>

            <!-- CONFIRM CARD -->
            <div class="form-card">
                <p style="font-size:14px; color:var(--text-muted); margin-bottom:24px;">
                    Estás a punto de eliminar al usuario <strong style="color:var(--text-dark);">{{ object.username }}</strong>.
                    ¿Deseas continuar?
                </p>
                <form method="post">
                    {% csrf_token %}
                    <div class="form-actions">
                        <a href="{% url 'usuarios_lista' %}" class="btn-secondary">Cancelar</a>
                        <button type="submit" class="btn-danger">Confirmar eliminación</button>
                    </div>
                </form>
            </div>

        </section>
    </main>
</div>


    <script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>
    <script>feather.replace({'stroke-width': 2})</script>
</body>
</html>

usuarios_form.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Usuario | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body>

<div class="layout">

    <!-- SIDEBAR -->
    <aside class="sidebar">
        <div class="profile">
            <img src="{% static 'users/img/profile.png' %}" alt="Perfil">
            <h4>{{ request.user.get_full_name|default:request.user.username }}</h4>
            <p>Administrador Global</p>
        </div>
        <nav class="menu">
            <a href="{% url 'dashboard' %}">
                <i data-feather="grid"></i>
                Dashboard
            </a>
            <a href="{% url 'usuarios_lista' %}" class="active">
                <i data-feather="users"></i>
                Usuarios
            </a>
            <a href="{% url 'entidades_lista' %}">
                <i data-feather="home"></i>
                Entidades
            </a>
            <a href="{% url 'saq' %}">
                <i data-feather="file-text"></i>
                SAQ
            </a>
            <a href="#">
                <i data-feather="check-square"></i>
                Pre-Test
            </a>
            <a href="#">
                <i data-feather="paperclip"></i>
                Evidencias
            </a>
            <a href="#">
                <i data-feather="star"></i>
                Renovación
            </a>
            <a href="{% url 'logout' %}" class="logout">
                <i data-feather="log-out"></i>
                Cerrar Sesión
            </a>
        </nav>
    </aside>

    <!-- MAIN -->
    <main class="main-content">

    <!-- TOPBAR -->
    <header class="topbar">

        <nav class="topbar-nav">
            <a href="{% url 'dashboard' %}">Dashboard</a>
            <a href="#">Catálogos</a>
            <a href="#">Configuración</a>
            <a href="#">Ayuda</a>
        </nav>

        <div class="topbar-right">

            <div class="topbar-search">
                <i data-feather="search"></i>
                <input type="text" placeholder="Buscar...">
            </div>

            <div class="topbar-lang">
                Eng ▾
            </div>

        </div>

    </header>

        <!-- CONTENT -->
        <section class="content">

            <!-- PAGE HEADER -->
            <div class="page-header">
                <div class="page-header-text">
                    <h1>{% if form.instance.pk %}Editar Usuario{% else %}Nuevo Usuario{% endif %}</h1>
                    <p>
                        {% if form.instance.pk %}
                            Modifica los datos del usuario, su rol y estado de acceso.
                        {% else %}
                            Completa los campos para registrar un nuevo usuario en el sistema.
                        {% endif %}
                    </p>
                </div>
                <div class="page-header-pcilogo-min">
                    <img src="{% static 'users/img/pcilogo.png' %}" alt="PCI Cert Pro">
                </div>
            </div>

            <!-- FORM CARD -->
            <div class="form-card">
                <form method="post" class="form-modern">
                    {% csrf_token %}
                    {{ form.as_p }}
                    <div class="form-actions">
                        <a href="{% url 'usuarios_lista' %}" class="btn-secondary">Cancelar</a>
                        <button type="submit" class="btn-primary">Guardar</button>
                    </div>
                </form>
            </div>

        </section>
    </main>
</div>


    <script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>
    <script>feather.replace({'stroke-width': 2})</script>
</body>
</html>

usuarios_lista.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Usuarios | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body>

<div class="layout">

    <!-- SIDEBAR -->
    <aside class="sidebar">
        <div class="profile">
            <img src="{% static 'users/img/profile.png' %}" alt="Perfil">
            <h4>{{ request.user.get_full_name|default:request.user.username }}</h4>
            <p>Administrador Global</p>
        </div>
        <nav class="menu">
            <a href="{% url 'dashboard' %}">
                <i data-feather="grid"></i>
                Dashboard
            </a>
            <a href="{% url 'usuarios_lista' %}" class="active">
                <i data-feather="users"></i>
                Usuarios
            </a>
            <a href="{% url 'entidades_lista' %}">
                <i data-feather="home"></i>
                Entidades
            </a>
            <a href="{% url 'saq' %}">
                <i data-feather="file-text"></i>
                SAQ
            </a>
            <a href="#">
                <i data-feather="check-square"></i>
                Pre-Test
            </a>
            <a href="#">
                <i data-feather="paperclip"></i>
                Evidencias
            </a>
            <a href="#">
                <i data-feather="star"></i>
                Renovación
            </a>
            <a href="{% url 'logout' %}" class="logout">
                <i data-feather="log-out"></i>
                Cerrar Sesión
            </a>
        </nav>
    </aside>

    <!-- MAIN -->
    <main class="main-content">

    <!-- TOPBAR -->
    <header class="topbar">

        <nav class="topbar-nav">
            <a href="{% url 'dashboard' %}">Dashboard</a>
            <a href="#">Catálogos</a>
            <a href="#">Configuración</a>
            <a href="#">Ayuda</a>
        </nav>

        <div class="topbar-right">

            <div class="topbar-search">
                <i data-feather="search"></i>
                <input type="text" placeholder="Buscar...">
            </div>

            <div class="topbar-lang">
                Eng ▾
            </div>

        </div>

    </header>

        <!-- CONTENT -->
        <section class="content">

            <!-- PAGE HEADER -->
            <div class="page-header">
                <div class="page-header-text">
                    <h1>Administración de usuarios y roles</h1>
                    <p>Crea, edita o elimina usuarios, define roles con distintos niveles de acceso y gestiona la autenticación
                    de manera centralizada para mantener el control y la trazabilidad de acciones.</p>
                </div>
                <div class="page-header-logo-min">
                    <img src="{% static 'users/img/logo.png' %}" alt="PCI Cert Pro">
                </div>
            </div>

            <!-- ADD BUTTON -->
            <div style="display:flex; justify-content:flex-end; margin-bottom: 16px;">
                <a href="{% url 'usuario_crear' %}" class="btn-add" title="Nuevo Usuario">
                    <i data-feather="plus" style="width:20px;height:20px;stroke-width:2.5"></i>
                </a>
            </div>

            <!-- TABLE CONTROLS -->
            <div class="table-controls">
                <div class="table-controls-left">
                    <label>Mostrar</label>
                    <form method="get" style="display:flex;align-items:center;gap:8px;">
                        <input type="number" name="per_page" value="{{ request.GET.per_page|default:'10' }}" min="5" max="100" style="width:60px;">
                        <label>registros</label>
                    </form>
                </div>
                <div class="table-controls-right">
                    <label>Buscar</label>
                    <form method="get" class="search-box">
                        <input type="text" name="buscar" placeholder="" value="{{ request.GET.buscar }}">
                    </form>
                </div>
            </div>

            <!-- TABLE -->
            <table class="modern-table">
                <thead>
                    <tr>
                        <th>Apellidos <span class="sort-icon">↑↓</span></th>
                        <th>Nombre <span class="sort-icon">↑↓</span></th>
                        <th>Nombre de usuario <span class="sort-icon">↑↓</span></th>
                        <th>Correo <span class="sort-icon">↑↓</span></th>
                        <th>Confirmado <span class="sort-icon">↑↓</span></th>
                        <th>Roles <span class="sort-icon">↑↓</span></th>
                        <th>Última modificación <span class="sort-icon">↑↓</span></th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    {% for user in usuarios %}
                    <tr>
                        <td>{{ user.last_name }}</td>
                        <td>{{ user.first_name }}</td>
                        <td>{{ user.username }}</td>
                        <td>{{ user.email }}</td>
                        <td>
                            {% if user.is_active %}
                                <span class="badge badge-success">Sí</span>
                            {% else %}
                                <span class="badge badge-danger">No</span>
                            {% endif %}
                        </td>
                        <td>
                            {% if user.is_superuser %}Administrador Global
                            {% else %}Usuario{% endif %}
                        </td>
                        <td style="color: var(--text-muted);">—</td>
                        <td>
                            <div class="actions">
                                <a href="{% url 'usuario_editar' user.pk %}" class="btn-icon" title="Ver">
                                    <i data-feather="search" style="width:14px;height:14px"></i>
                                </a>
                                <a href="{% url 'usuario_editar' user.pk %}" class="btn-icon" title="Editar">
                                    <i data-feather="edit-2" style="width:14px;height:14px"></i>
                                </a>
                                <a href="{% url 'usuario_eliminar' user.pk %}" class="btn-icon btn-icon-delete" title="Eliminar">
                                    <i data-feather="trash-2" style="width:14px;height:14px"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                    {% empty %}
                    <tr>
                        <td colspan="8" style="text-align:center; color:var(--text-muted); padding:30px;">
                            No hay usuarios registrados.
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>

            <!-- PAGINATION -->
            <div class="pagination">
                <div class="pagination-info">
                    Mostrando registros del {{ page_obj.start_index }} al {{ page_obj.end_index }}
                    de un total de {{ page_obj.paginator.count }} registros
                </div>
                <div class="pagination-controls">
                    {% if page_obj.has_previous %}
                        <a href="?page={{ page_obj.previous_page_number }}&buscar={{ request.GET.buscar }}">Anterior</a>
                    {% else %}
                        <span style="opacity:0.4;">Anterior</span>
                    {% endif %}

                    {% for num in page_obj.paginator.page_range %}
                        {% if num == page_obj.number %}
                            <span class="current">{{ num }}</span>
                        {% elif num|add:"-1" == page_obj.number or num|add:"1" == page_obj.number %}
                            <a href="?page={{ num }}&buscar={{ request.GET.buscar }}">{{ num }}</a>
                        {% endif %}
                    {% endfor %}

                    {% if page_obj.has_next %}
                        <a href="?page={{ page_obj.next_page_number }}&buscar={{ request.GET.buscar }}">Siguiente</a>
                    {% else %}
                        <span style="opacity:0.4;">Siguiente</span>
                    {% endif %}
                </div>
            </div>

        </section>
    </main>
</div>


    <script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>
    <script>feather.replace({'stroke-width': 2})</script>
</body>
</html>
