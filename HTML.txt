dashboard.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Dashboard | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body>

<div class="layout">

    <aside class="sidebar">
        <div class="profile">
            <img src="{% static 'users/img/profile.png' %}" alt="Perfil">
            <h4>{{ request.user.get_full_name|default:request.user.username }}</h4>
            <p>Administrador Global</p>
        </div>
        <nav class="menu">
            <a href="{% url 'dashboard' %}" class="active">
                <i data-feather="grid"></i>
                Dashboard
            </a>
            <a href="{% url 'usuarios_lista' %}">
                <i data-feather="users"></i>
                Usuarios
            </a>
            <a href="{% url 'entidades_lista' %}">
                <i data-feather="home"></i>
                Entidades
            </a>
            <a href="{% url 'saq_lista' %}">
                <i data-feather="file-text"></i>
                SAQ
            </a>
            <a href="#"><i data-feather="check-square"></i> Pre-Test</a>
            <a href="#"><i data-feather="paperclip"></i> Evidencias</a>
            <a href="#"><i data-feather="star"></i> Renovación</a>
            <a href="{% url 'logout' %}" class="logout">
                <i data-feather="log-out"></i>
                Cerrar Sesión
            </a>
        </nav>
    </aside>

    <main class="main-content">

        <header class="topbar">
            <nav class="topbar-nav">
                <a href="{% url 'dashboard' %}" class="active">Dashboard</a>
                <a href="#">Catálogos</a>
                <a href="#">Configuración</a>
                <a href="#">Ayuda</a>
            </nav>

            <div class="topbar-right">
                <div class="topbar-search">
                    <i data-feather="search"></i>
                    <input type="text" placeholder="Buscar...">
                </div>
                <div class="topbar-lang">Eng ▾</div>
            </div>
        </header>

        <section class="content">
            <div class="page-header">
                <div class="page-header-text">
                    <h1>¡Bienvenido a PCI Cert Pro!</h1>
                    <p>
                        Tu plataforma integral para la autocertificación en cumplimiento con el estándar PCI DSS v4.0.1.<br>
                        Gestiona todo el proceso de certificación de manera segura, trazable y automatizada.
                    </p>
                </div>
                <div class="page-header-logo">
                    <img src="{% static 'users/img/logo.png' %}" alt="PCI Cert Pro">
                </div>
            </div>
        </section>

    </main>
</div>

<script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>
<script>feather.replace({'stroke-width': 2})</script>
</body>
</html>

entidades_detalle.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{ entidad.nombre_empresa }}</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body>

<div class="layout">

<aside class="sidebar">
    <nav class="menu">
        <a href="{% url 'dashboard' %}">Dashboard</a>
        <a href="{% url 'usuarios_lista' %}">Usuarios</a>
        <a href="{% url 'entidades_lista' %}" class="active">Entidades</a>
        <a href="{% url 'saq_lista' %}">SAQ</a>
    </nav>
</aside>

<main class="main-content">

<section class="content">

<h1>{{ entidad.nombre_empresa }}</h1>

<p><strong>Usuario:</strong> {{ entidad.usuario.username }}</p>
<p><strong>Email:</strong> {{ entidad.email }}</p>

<a href="{% url 'entidad_editar' entidad.pk %}">Editar</a>

</section>

</main>
</div>
</body>
</html>

entidades_form.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Entidad | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body>
<div class="layout">

    <aside class="sidebar">
        <div class="profile">
            <img src="{% static 'users/img/profile.png' %}" alt="Perfil">
            <h4>{{ request.user.get_full_name|default:request.user.username }}</h4>
            <p>Administrador Global</p>
        </div>
        <nav class="menu">
            <a href="{% url 'dashboard' %}"><i data-feather="grid"></i> Dashboard</a>
            <a href="{% url 'usuarios_lista' %}"><i data-feather="users"></i> Usuarios</a>
            <a href="{% url 'entidades_lista' %}" class="active"><i data-feather="home"></i> Entidades</a>
            <a href="{% url 'saq_lista' %}"><i data-feather="file-text"></i> SAQ</a>
            <a href="#"><i data-feather="check-square"></i> Pre-Test</a>
            <a href="#"><i data-feather="paperclip"></i> Evidencias</a>
            <a href="#"><i data-feather="star"></i> Renovación</a>
            <a href="{% url 'logout' %}" class="logout"><i data-feather="log-out"></i> Cerrar Sesión</a>
        </nav>
    </aside>

    <main class="main-content">
        <header class="topbar">
            <nav class="topbar-nav">
                <a href="{% url 'dashboard' %}">Dashboard</a>
                <a href="#">Catálogos</a>
                <a href="#">Configuración</a>
                <a href="#">Ayuda</a>
            </nav>
        </header>

        <section class="content">
            <div class="page-header">
                <h1>{% if form.instance.pk %}Editar Entidad{% else %}Nueva Entidad{% endif %}</h1>
            </div>

            <div class="form-card">
                <form method="post">
                    {% csrf_token %}
                    {{ form.as_p }}
                    <div class="form-actions">
                        <a href="{% url 'entidades_lista' %}">Cancelar</a>
                        <button type="submit">Guardar</button>
                    </div>
                </form>
            </div>
        </section>
    </main>
</div>
</body>
</html>

entidades_lista.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Entidades | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body>
<div class="layout">

    <aside class="sidebar">
        <div class="profile">
            <img src="{% static 'users/img/profile.png' %}" alt="Perfil">
            <h4>{{ request.user.get_full_name|default:request.user.username }}</h4>
            <p>
                {% if request.user.is_superuser %}
                    Administrador Global
                {% else %}
                    {{ request.user.groups.first.name|default:"Usuario" }}
                {% endif %}
            </p>
        </div>

        <nav class="menu">
            <a href="{% url 'dashboard' %}"><i data-feather="grid"></i> Dashboard</a>
            <a href="{% url 'usuarios_lista' %}"><i data-feather="users"></i> Usuarios</a>
            <a href="{% url 'entidades_lista' %}" class="active"><i data-feather="home"></i> Entidades</a>
            <a href="{% url 'saq_lista' %}"><i data-feather="file-text"></i> SAQ</a>
            <a href="#"><i data-feather="check-square"></i> Pre-Test</a>
            <a href="#"><i data-feather="paperclip"></i> Evidencias</a>
            <a href="#"><i data-feather="star"></i> Renovación</a>
            <a href="{% url 'logout' %}" class="logout"><i data-feather="log-out"></i> Cerrar Sesión</a>
        </nav>
    </aside>

    <main class="main-content">

        <header class="topbar">
            <nav class="topbar-nav">
                <a href="{% url 'dashboard' %}">Dashboard</a>
                <a href="#">Catálogos</a>
                <a href="#">Configuración</a>
                <a href="#">Ayuda</a>
            </nav>
            <div class="topbar-right">
                <div class="topbar-search">
                    <i data-feather="search"></i>
                    <input type="text" placeholder="Buscar...">
                </div>
                <div class="topbar-lang">Eng ▾</div>
            </div>
        </header>

        <section class="content">

            <!-- HEADER -->
            <div class="page-header">
                <div class="page-header-text">
                    <h1>Administración de entidades</h1>
                    <p>Visualiza, edita y activa o desactiva entidades registradas.</p>
                </div>

                <div style="display:flex; align-items:center; gap:15px;">
                    <a href="{% url 'entidad_crear' %}" class="btn-primary">
                        + Nueva Entidad
                    </a>
                    <div class="page-header-logo-min">
                        <img src="{% static 'users/img/logo.png' %}">
                    </div>
                </div>
            </div>

            <!-- CONTROLES -->
            <div class="table-controls">
                <div class="table-controls-left">
                    <form method="get">
                        <input 
                            type="text" 
                            name="buscar" 
                            placeholder="Buscar entidad..." 
                            value="{{ request.GET.buscar }}"
                        >
                    </form>
                </div>
            </div>

            <!-- TABLA -->
            <table class="modern-table">
                <thead>
                    <tr>
                        <td>Empresa</td>
                        <td>DBA</td>
                        <td>Email</td>
                        <td>Contacto</td>
                        <td>Estado</td>
                        <td>Acciones</td>
                    </tr>
                </thead>

                <tbody>
                    {% for entidad in entidades %}
                    <tr>
                        <td>{{ entidad.nombre_empresa }}</td>
                        <td>{{ entidad.dba }}</td>
                        <td>{{ entidad.email }}</td>
                        <td>{{ entidad.contacto }}</td>

                        <td>
                            {% if entidad.is_active %}
                                <span class="badge badge-success">Activa</span>
                            {% else %}
                                <span class="badge badge-danger">Inactiva</span>
                            {% endif %}
                        </td>

                        <td>
                            <div class="actions">
                                <a href="{% url 'entidad_editar' entidad.pk %}" class="btn-icon">
                                    <i data-feather="edit"></i>
                                </a>

                                <a href="{% url 'entidad_toggle' entidad.pk %}" class="btn-icon">
                                    {% if entidad.is_active %}
                                        <i data-feather="toggle-right"></i>
                                    {% else %}
                                        <i data-feather="toggle-left"></i>
                                    {% endif %}
                                </a>
                            </div>
                        </td>
                    </tr>
                    {% empty %}
                    <tr>
                        <td colspan="6">No hay entidades registradas.</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>

            <!-- PAGINACIÓN -->
            <div class="pagination">
                <div>
                    Página {{ page_obj.number }} de {{ page_obj.paginator.num_pages }}
                </div>

                <div class="pagination-controls">
                    {% if page_obj.has_previous %}
                        <a href="?page={{ page_obj.previous_page_number }}">Anterior</a>
                    {% endif %}

                    <span class="current">{{ page_obj.number }}</span>

                    {% if page_obj.has_next %}
                        <a href="?page={{ page_obj.next_page_number }}">Siguiente</a>
                    {% endif %}
                </div>
            </div>

        </section>
    </main>
</div>

<script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>
<script>feather.replace({'stroke-width': 2})</script>
</body>
</html>

forgot_password.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Recuperar Contraseña | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body class="login-page">

    <section class="login-hero">
        <img src="{% static 'users/img/banner.jpg' %}" alt="Banner">
        <div class="login-hero-overlay">
            <img src="{% static 'users/img/logo.png' %}" class="login-logo">
        </div>
    </section>

    <section class="login-wrapper">
        <div class="login-card">

            <h2 style="font-size:17px; margin-bottom:4px;">Recuperar contraseña</h2>
            <p style="font-size:13px; color:var(--text-muted); margin-bottom:8px;">
                Ingresa tu correo y te enviaremos un enlace para restablecer tu contraseña.
            </p>

            {% if message %}
                <div class="login-error" style="color:var(--success); background:var(--success-bg);">
                    {{ message }}
                </div>
            {% endif %}

            {% if error %}
                <div class="login-error">{{ error }}</div>
            {% endif %}

            <form method="post">
                {% csrf_token %}
                <label>Correo electrónico</label>
                <input type="email" name="email" required placeholder="tucorreo@empresa.com">
                <button type="submit" class="btn-primary" style="width:100%; margin-top:14px;">
                    Enviar enlace
                </button>
            </form>

            <div class="forgot" style="margin-top:14px;">
                <a href="{% url 'login' %}">← Volver al inicio de sesión</a>
            </div>

        </div>
    </section>

</body>
</html>

login.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Login | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body class="login-page">

    <!-- HERO / BANNER -->
    <section class="login-hero">
        <img src="{% static 'users/img/banner.jpg' %}" alt="Banner">

        <div class="login-hero-overlay">
            <img src="{% static 'users/img/logo.png' %}" class="login-logo">
        </div>
    </section>

    <!-- LOGIN -->
    <section class="login-wrapper">

        <div class="login-card">

            <form method="post">
                {% csrf_token %}

                {% if error %}
                    <div class="login-error">{{ error }}</div>
                {% endif %}

                <label>Nombre de usuario</label>
                <input type="text" name="username" required>

                <label>Contraseña</label>
                <input type="password" name="password" required>

                <label class="remember">
                    <input type="checkbox" name="remember">
                    Recordarme
                </label>

                <button type="submit" class="btn-primary">
                    Iniciar Sesión
                </button>

                <div class="forgot">
                    <a href="{% url 'forgot_password' %}">¿Olvidaste tu contraseña?</a>
                </div>

            </form>

        </div>

    </section>

</body>
</html>

pretest.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Pre-Test | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body>
<div class="layout">

    <aside class="sidebar">
        <div class="profile">
            <img src="{% static 'users/img/profile.png' %}">
            <h4>{{ request.user.get_full_name|default:request.user.username }}</h4>
        </div>
        <nav class="menu">
            <a href="{% url 'dashboard' %}"><i data-feather="grid"></i> Dashboard</a>
            <a href="{% url 'usuarios_lista' %}"><i data-feather="users"></i> Usuarios</a>
            <a href="{% url 'entidades_lista' %}"><i data-feather="home"></i> Entidades</a>
            <a href="{% url 'saq_lista' %}"><i data-feather="file-text"></i> SAQ</a>
            <a href="{% url 'pretest' %}" class="active"><i data-feather="check-square"></i> Pre-Test</a>
            <a href="{% url 'logout' %}" class="logout"><i data-feather="log-out"></i> Cerrar sesión</a>
        </nav>
    </aside>

    <main class="main-content">

        <header class="topbar">
            <nav class="topbar-nav">
                <a href="{% url 'dashboard' %}">Dashboard</a>
                <a href="#" class="active">Pre-Test</a>
            </nav>
        </header>

        <section class="content">

            <div class="page-header">
                <div class="page-header-text">
                    <h1>Pre-Test PCI DSS</h1>
                    <p>Responde las siguientes preguntas para determinar qué tipo de SAQ aplica a tu organización.</p>
                </div>
            </div>

            <form method="POST">
                {% csrf_token %}

                {% for p in preguntas %}
                <div class="card" style="margin-bottom:12px; padding:16px;">
                    <p style="margin-bottom:10px;">
                        <strong>{{ p.numero }}.</strong> {{ p.texto_es }}
                    </p>
                    <div style="display:flex; gap:20px;">
                        <label>
                            <input type="radio" name="p_{{ p.id }}" value="SI" required> Sí
                        </label>
                        <label>
                            <input type="radio" name="p_{{ p.id }}" value="NO"> No
                        </label>
                    </div>
                </div>
                {% endfor %}

                <div class="form-actions" style="margin-top:20px;">
                    <button type="submit" class="btn-primary">Evaluar →</button>
                </div>

            </form>

        </section>
    </main>
</div>

<script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>
<script>feather.replace({'stroke-width': 2})</script>
</body>
</html>

pretest_resultado.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Resultado Pre-Test | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body>
<div class="layout">

    <aside class="sidebar">
        <div class="profile">
            <img src="{% static 'users/img/profile.png' %}">
            <h4>{{ request.user.get_full_name|default:request.user.username }}</h4>
        </div>
        <nav class="menu">
            <a href="{% url 'dashboard' %}"><i data-feather="grid"></i> Dashboard</a>
            <a href="{% url 'usuarios_lista' %}"><i data-feather="users"></i> Usuarios</a>
            <a href="{% url 'entidades_lista' %}"><i data-feather="home"></i> Entidades</a>
            <a href="{% url 'saq_lista' %}"><i data-feather="file-text"></i> SAQ</a>
            <a href="{% url 'pretest' %}" class="active"><i data-feather="check-square"></i> Pre-Test</a>
            <a href="{% url 'logout' %}" class="logout"><i data-feather="log-out"></i> Cerrar sesión</a>
        </nav>
    </aside>

    <main class="main-content">

        <header class="topbar">
            <nav class="topbar-nav">
                <a href="{% url 'dashboard' %}">Dashboard</a>
                <a href="{% url 'pretest' %}">Pre-Test</a>
                <a href="#" class="active">Resultado</a>
            </nav>
        </header>

        <section class="content">

            <div class="page-header">
                <div class="page-header-text">
                    <h1>Resultado del Pre-Test</h1>
                    <p>Basado en tus respuestas, este es el SAQ recomendado para tu organización.</p>
                </div>
            </div>

            <!-- RESULTADO PRINCIPAL -->
            <div class="card" style="margin-bottom:20px; padding:24px; text-align:center;">
                <p style="font-size:14px; color:var(--text-muted);">SAQ Recomendado</p>
                <h2 style="font-size:36px; margin:8px 0;">{{ recomendacion }}</h2>
                <p style="color:var(--text-muted);">
                    {{ total }} preguntas respondidas &nbsp;·&nbsp;
                    {{ si }} afirmativas &nbsp;·&nbsp;
                    {{ no }} negativas
                </p>
            </div>

            <!-- DETALLE POR SAQ -->
            <div class="card" style="padding:20px;">
                <h3 style="margin-bottom:16px;">Detalle por tipo de SAQ</h3>
                {% for saq, count in saq_scores.items %}
                <div class="detail-row">
                    <span class="label">{{ saq }}</span>
                    <span class="value">{{ count }} respuesta{{ count|pluralize:"s" }} afirmativa{{ count|pluralize:"s" }}</span>
                </div>
                {% endfor %}
            </div>

            <div style="margin-top:20px; display:flex; gap:12px;">
                <a href="{% url 'pretest' %}" class="btn-secondary">← Repetir Pre-Test</a>
                <a href="{% url 'saq_lista' %}" class="btn-primary">Ver SAQ →</a>
            </div>

        </section>
    </main>
</div>

<script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>
<script>feather.replace({'stroke-width': 2})</script>
</body>
</html>

reset_password.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Nueva Contraseña | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body class="login-page">

    <section class="login-hero">
        <img src="{% static 'users/img/banner.jpg' %}" alt="Banner">
        <div class="login-hero-overlay">
            <img src="{% static 'users/img/logo.png' %}" class="login-logo">
        </div>
    </section>

    <section class="login-wrapper">
        <div class="login-card">

            {% if validlink %}
                <h2 style="font-size:17px; margin-bottom:4px;">Nueva contraseña</h2>
                <p style="font-size:13px; color:var(--text-muted); margin-bottom:8px;">
                    Ingresa tu nueva contraseña dos veces para confirmarla.
                </p>

                <form method="post">
                    {% csrf_token %}
                    {% for field in form %}
                        <label>{{ field.label }}</label>
                        {{ field }}
                        {% if field.errors %}
                            <div class="login-error">{{ field.errors }}</div>
                        {% endif %}
                    {% endfor %}
                    <button type="submit" class="btn-primary" style="width:100%; margin-top:14px;">
                        Guardar contraseña
                    </button>
                </form>

            {% else %}
                <h2 style="font-size:17px; margin-bottom:8px;">Enlace inválido</h2>
                <p style="font-size:13px; color:var(--text-muted);">
                    Este enlace ya fue usado o ha expirado. Solicita uno nuevo.
                </p>
                <div style="margin-top:14px;">
                    <a href="{% url 'forgot_password' %}" class="btn-primary"
                       style="display:block; width:100%; text-align:center; text-decoration:none; padding:10px;">
                        Solicitar nuevo enlace
                    </a>
                </div>
            {% endif %}

            <div class="forgot" style="margin-top:14px;">
                <a href="{% url 'login' %}">← Volver al inicio de sesión</a>
            </div>

        </div>
    </section>

</body>
</html>

reset_password_confirm.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Nueva Contraseña | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body class="login-page">

    <section class="login-hero">
        <img src="{% static 'users/img/banner.jpg' %}" alt="Banner">
        <div class="login-hero-overlay">
            <img src="{% static 'users/img/logo.png' %}" class="login-logo">
        </div>
    </section>

    <section class="login-wrapper">
        <div class="login-card">

            {% if validlink %}
                <h2 style="font-size:17px; margin-bottom:4px;">Nueva contraseña</h2>
                <p style="font-size:13px; color:var(--text-muted); margin-bottom:8px;">
                    Ingresa tu nueva contraseña dos veces para confirmarla.
                </p>
                <form method="post">
                    {% csrf_token %}
                    {% for field in form %}
                        <label>{{ field.label }}</label>
                        {{ field }}
                        {% if field.errors %}
                            <div class="login-error">{{ field.errors }}</div>
                        {% endif %}
                    {% endfor %}
                    <button type="submit" class="btn-primary" style="width:100%; margin-top:14px;">
                        Guardar contraseña
                    </button>
                </form>

            {% else %}
                <h2 style="font-size:17px; margin-bottom:8px;">Enlace inválido</h2>
                <p style="font-size:13px; color:var(--text-muted);">
                    Este enlace ya fue usado o ha expirado. Solicita uno nuevo.
                </p>
                <div style="margin-top:14px;">
                    <a href="{% url 'forgot_password' %}" class="btn-primary"
                       style="display:block; width:100%; text-align:center; text-decoration:none; padding:10px;">
                        Solicitar nuevo enlace
                    </a>
                </div>
            {% endif %}

            <div class="forgot" style="margin-top:14px;">
                <a href="{% url 'login' %}">← Volver al inicio de sesión</a>
            </div>

        </div>
    </section>

</body>
</html>

saq_detalle_seccion.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<title>Editor SAQ | PCI Cert Pro</title>
<link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>

<body>

<div class="layout">

<!-- SIDEBAR -->
<aside class="sidebar">
    <div class="profile">
        <img src="{% static 'users/img/profile.png' %}">
        <h4>{{ request.user.get_full_name|default:request.user.username }}</h4>
        <p>
            {% if request.user.is_superuser %}
                Administrador Global
            {% else %}
                {{ request.user.groups.first.name|default:"Usuario" }}
            {% endif %}
        </p>
    </div>

    <nav class="menu">
        <a href="{% url 'dashboard' %}">Dashboard</a>
        <a href="{% url 'usuarios_lista' %}">Usuarios</a>
        <a href="{% url 'entidades_lista' %}">Entidades</a>
        <a href="{% url 'saq_lista' %}" class="active">SAQ</a>
        <a href="{% url 'pretest' %}">Pre-Test</a>
        <a href="{% url 'logout' %}" class="logout">Cerrar sesión</a>
    </nav>
</aside>

<!-- MAIN -->
<main class="main-content">

<header class="topbar">
    <h2>{{ tipo.nombre }}</h2>
</header>

<section class="content">

<div style="display:flex; gap:20px;">

<!-- SECCIONES -->
<div class="card" style="width:260px; flex-shrink:0;">
    <div class="card-header">
        <h3>Secciones</h3>
    </div>

    <div class="card-body" style="display:flex; flex-direction:column; gap:8px;">
        {% for s in secciones %}
            <a href="{% url 'saq_detalle_seccion' tipo.pk s.pk %}"
               class="btn-secondary {% if seccion_activa and s.pk == seccion_activa.pk %}active{% endif %}">
               {{ s.nombre }}
            </a>
        {% endfor %}
    </div>
</div>

<!-- EDITOR -->
<div class="card" style="flex:1;">

    <div class="card-header">
        <h3>
            {% if seccion_activa %}
                {{ seccion_activa.nombre }}
            {% else %}
                Selecciona una sección
            {% endif %}
        </h3>
    </div>

    <div class="card-body">

    {% if seccion_activa %}

        <!-- LISTA DE PREGUNTAS -->
        <div id="preguntas-container" style="display:flex; flex-direction:column; gap:10px;">

            {% for p in preguntas %}
            <div id="pregunta-{{ p.pk }}" class="card" style="padding:12px;">
                
                <div style="display:flex; justify-content:space-between; align-items:center;">
                    
                    <span>
                        {{ p.orden }}. {{ p.pregunta.texto }}
                    </span>

                    <button 
                        class="btn-icon danger btn-eliminar"
                        data-id="{{ p.pk }}"
                        data-url="{% url 'saq_pregunta_eliminar' tipo.pk seccion_activa.pk 0 %}">
                        🗑
                    </button>

                </div>

            </div>
            {% empty %}
                <p>No hay preguntas aún.</p>
            {% endfor %}

        </div>

        <!-- FORM CREAR -->
        <form id="form-pregunta"
              data-url="{% url 'saq_pregunta_ajax_crear' tipo.pk seccion_activa.pk %}"
              style="margin-top:20px;">

            {% csrf_token %}

            <div class="form-group">
                <label>Nueva pregunta</label>
                <input type="text" name="texto" required>
            </div>

            <div class="form-group">
                <label>Referencia PCI (opcional)</label>
                <input type="text" name="referencia_pci">
            </div>

            <div class="form-actions">
                <button type="submit" class="btn-primary">
                    + Agregar pregunta
                </button>
            </div>

        </form>

    {% endif %}

    </div>

</div>

</div>

</section>

</main>
</div>

<!-- ICONOS -->
<script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>
<script>
feather.replace({'stroke-width': 2});
</script>

<!-- ========================= -->
<!-- JS CREAR PREGUNTA -->
<!-- ========================= -->
<script>
const form = document.getElementById("form-pregunta");

form?.addEventListener("submit", function(e){
    e.preventDefault();

    const data = new FormData(form);
    const url = form.dataset.url;

    fetch(url, {
        method:"POST",
        body:data,
        headers:{
            "X-CSRFToken": data.get("csrfmiddlewaretoken")
        }
    })
    .then(res => res.json())
    .then(data => {
        if(data.success){
            location.reload();
        }
    });
});
</script>

<!-- ========================= -->
<!-- JS ELIMINAR -->
<!-- ========================= -->
<script>
document.querySelectorAll(".btn-eliminar").forEach(btn=>{
    btn.addEventListener("click", function(){

        const id = this.dataset.id;
        const baseUrl = this.dataset.url;
        const url = baseUrl.replace("0", id);

        fetch(url,{
            method:"POST",
            headers:{
                "X-CSRFToken": document.querySelector('[name=csrfmiddlewaretoken]').value
            }
        })
        .then(res => res.json())
        .then(data => {
            if(data.success){
                document.getElementById("pregunta-" + id).remove();
            }
        });

    });
});
</script>

</body>
</html>

saq_editor.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Editor SAQ | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body>
<div class="layout">

    <aside class="sidebar">
        <div class="profile">
            <img src="{% static 'users/img/profile.png' %}">
            <h4>{{ request.user.get_full_name|default:request.user.username }}</h4>
            <p>
                {% if request.user.is_superuser %}
                    Administrador Global
                {% else %}
                    {{ request.user.groups.first.name|default:"Usuario" }}
                {% endif %}
            </p>
        </div>
        <nav class="menu">
            <a href="{% url 'dashboard' %}"><i data-feather="grid"></i> Dashboard</a>
            <a href="{% url 'usuarios_lista' %}"><i data-feather="users"></i> Usuarios</a>
            <a href="{% url 'entidades_lista' %}"><i data-feather="home"></i> Entidades</a>
            <a href="{% url 'saq_lista' %}" class="active"><i data-feather="file-text"></i> SAQ</a>
            <a href="{% url 'pretest' %}"><i data-feather="check-square"></i> Pre-Test</a>
            <a href="{% url 'logout' %}" class="logout"><i data-feather="log-out"></i> Cerrar sesión</a>
        </nav>
    </aside>

    <main class="main-content">
        <header class="topbar">
            <nav class="topbar-nav">
                <a href="{% url 'dashboard' %}">Dashboard</a>
                <a href="{% url 'saq_lista' %}">SAQ</a>
                <a href="#" class="active">{{ tipo.nombre }}</a>
            </nav>
        </header>

        <section class="content">

            <div class="page-header">
                <div class="page-header-text">
                    <h1>{{ tipo.nombre }}</h1>
                    <p>Editor de cuestionario</p>
                </div>
                <div class="page-actions">
                    <a href="{% url 'saq_lista' %}" class="btn-secondary">← Volver</a>
                </div>
            </div>

            <div style="display:flex; gap:20px;">

                <!-- SECCIONES -->
                <div class="card" style="width:280px; flex-shrink:0;">
                    <div class="card-header">
                        <h3>Secciones</h3>
                    </div>
                    <div class="card-body" style="display:flex; flex-direction:column; gap:8px;">
                        {% for s in secciones %}
                        <a href="{% url 'saq_editor_seccion' tipo.pk s.pk %}"
                           class="btn-secondary {% if s.pk == seccion_activa.pk %}seccion-activa{% endif %}"
                           style="text-align:left;">
                            {{ s.nombre }}
                        </a>
                        {% endfor %}
                    </div>
                </div>

                <!-- EDITOR -->
                <div class="card" style="flex:1;">
                    <div class="card-header">
                        <h3>{{ seccion_activa.nombre }}</h3>
                        <span style="font-size:13px; color:var(--text-muted);">
                            {{ preguntas|length }} pregunta{{ preguntas|length|pluralize:"s" }}
                        </span>
                    </div>

                    <div class="card-body">

                        <!-- LISTA DE PREGUNTAS -->
                        <div id="preguntas-container" style="display:flex; flex-direction:column; gap:10px;">
                            {% for p in preguntas %}
                            <div id="pregunta-{{ p.pk }}" class="card" style="padding:12px;">
                                <div style="display:flex; justify-content:space-between; align-items:flex-start; gap:12px;">
                                    <span style="font-size:14px; line-height:1.5;">
                                        <strong>{{ p.orden }}.</strong> {{ p.pregunta.texto }}
                                        {% if p.pregunta.referencia_pci %}
                                        <br><small style="color:var(--text-muted);">Ref: {{ p.pregunta.referencia_pci }}</small>
                                        {% endif %}
                                    </span>
                                    <button class="btn-icon danger btn-eliminar" data-id="{{ p.pk }}" style="flex-shrink:0;">
                                        <i data-feather="trash-2"></i>
                                    </button>
                                </div>
                            </div>
                            {% empty %}
                            <p style="color:var(--text-muted);">No hay preguntas en esta sección.</p>
                            {% endfor %}
                        </div>

                        <!-- FORM AGREGAR -->
                        <form id="form-pregunta" style="margin-top:24px; padding-top:20px; border-top:1px solid var(--border);">
                            {% csrf_token %}
                            <div class="form-group">
                                <label>Nueva pregunta</label>
                                <input type="text" name="texto" placeholder="Escribe el texto de la pregunta..." required>
                            </div>
                            <div class="form-group">
                                <label>Referencia PCI (opcional)</label>
                                <input type="text" name="referencia_pci" placeholder="Ej: 1.2.3">
                            </div>
                            <div class="form-actions">
                                <button type="submit" class="btn-primary">+ Agregar pregunta</button>
                            </div>
                        </form>

                    </div>
                </div>

            </div>
        </section>
    </main>
</div>

<script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>
<script>feather.replace({'stroke-width': 2})</script>

<script>
const CREAR_URL = "{% url 'saq_pregunta_crear' tipo.pk seccion_activa.pk %}";
const ELIMINAR_BASE = "{% url 'saq_pregunta_eliminar' tipo.pk seccion_activa.pk 1 %}";

// CREAR
document.getElementById('form-pregunta').addEventListener('submit', function(e) {
    e.preventDefault();
    const data = new FormData(this);

    fetch(CREAR_URL, {
        method: "POST",
        body: data,
        headers: { "X-CSRFToken": data.get("csrfmiddlewaretoken") }
    })
    .then(res => res.json())
    .then(data => { if (data.success) location.reload(); });
});

// ELIMINAR
document.querySelectorAll('.btn-eliminar').forEach(btn => {
    btn.addEventListener('click', function() {
        const id = this.dataset.id;
        const url = ELIMINAR_BASE.replace('/1/', `/${id}/`);

        fetch(url, {
            method: "POST",
            headers: { "X-CSRFToken": document.querySelector('[name=csrfmiddlewaretoken]').value }
        })
        .then(res => res.json())
        .then(data => {
            if (data.success) document.getElementById(`pregunta-${id}`).remove();
        });
    });
});
</script>

</body>
</html>

saq_lista.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>SAQ | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body>
<div class="layout">

    <aside class="sidebar">
        <div class="profile">
            <img src="{% static 'users/img/profile.png' %}">
            <h4>{{ request.user.get_full_name|default:request.user.username }}</h4>
            <p>
                {% if request.user.is_superuser %}
                    Administrador Global
                {% else %}
                    {{ request.user.groups.first.name|default:"Usuario" }}
                {% endif %}
            </p>
        </div>
        <nav class="menu">
            <a href="{% url 'dashboard' %}"><i data-feather="grid"></i> Dashboard</a>
            <a href="{% url 'usuarios_lista' %}"><i data-feather="users"></i> Usuarios</a>
            <a href="{% url 'entidades_lista' %}"><i data-feather="home"></i> Entidades</a>
            <a href="{% url 'saq_lista' %}" class="active"><i data-feather="file-text"></i> SAQ</a>
            <a href="{% url 'pretest' %}"><i data-feather="check-square"></i> Pre-Test</a>
            <a href="{% url 'logout' %}" class="logout"><i data-feather="log-out"></i> Cerrar sesión</a>
        </nav>
    </aside>

    <main class="main-content">
        <header class="topbar">
            <nav class="topbar-nav">
                <a href="{% url 'dashboard' %}">Dashboard</a>
                <a href="#" class="active">SAQ</a>
            </nav>
        </header>

        <section class="content">
            <div class="page-header">
                <div class="page-header-text">
                    <h1>Tipos de SAQ</h1>
                    <p>Selecciona un tipo para editar sus secciones y preguntas.</p>
                </div>
            </div>

            <div class="card-grid">
                {% for tipo in tipos %}
                <a href="{% url 'saq_editor' tipo.pk %}" style="text-decoration:none; color:inherit;">
                    <div class="card">
                        <div class="card-header">
                            <h3>{{ tipo.nombre }}</h3>
                        </div>
                        <div class="detail-row">
                            <span class="label">Secciones</span>
                            <span class="value">{{ tipo.secciones.count }}</span>
                        </div>
                        <div class="detail-row">
                            <span class="label">Preguntas</span>
                            <span class="value">{{ tipo.secciones.all|length }}</span>
                        </div>
                    </div>
                </a>
                {% empty %}
                <div class="card">
                    <p>No hay tipos de SAQ registrados.</p>
                </div>
                {% endfor %}
            </div>

        </section>
    </main>
</div>
<script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>
<script>feather.replace({'stroke-width': 2})</script>
</body>
</html>

usuario_detalle.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{ usuario.username }}</title>
</head>
<body>

<h1>{{ usuario.username }}</h1>

<p>Email: {{ usuario.email }}</p>

<a href="{% url 'usuario_editar' usuario.pk %}">Editar</a>

</body>
</html>

usuarios_form.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Usuario</title>
</head>
<body>

<h1>Formulario Usuario</h1>

<form method="post">
{% csrf_token %}
{{ form.as_p }}

<button type="submit">Guardar</button>
</form>

</body>
</html>

usuarios_lista.html

{% load static %}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Usuarios | PCI Cert Pro</title>
    <link rel="stylesheet" href="{% static 'users/css/main.css' %}">
</head>
<body>
<div class="layout">

    <aside class="sidebar">
        <div class="profile">
            <img src="{% static 'users/img/profile.png' %}" alt="Perfil">
            <h4>{{ request.user.get_full_name|default:request.user.username }}</h4>
            <p>
    {% if request.user.is_superuser %}
        Administrador Global
    {% else %}
        {{ request.user.groups.first.name|default:"Usuario" }}
    {% endif %}
            </p>
        </div>
        <nav class="menu">
            <a href="{% url 'dashboard' %}"><i data-feather="grid"></i> Dashboard</a>
            <a href="{% url 'usuarios_lista' %}" class="active"><i data-feather="users"></i> Usuarios</a>
            <a href="{% url 'entidades_lista' %}"><i data-feather="home"></i> Entidades</a>
            <a href="{% url 'saq_lista' %}"><i data-feather="file-text"></i> SAQ</a>
            <a href="#"><i data-feather="check-square"></i> Pre-Test</a>
            <a href="#"><i data-feather="paperclip"></i> Evidencias</a>
            <a href="#"><i data-feather="star"></i> Renovación</a>
            <a href="{% url 'logout' %}" class="logout"><i data-feather="log-out"></i> Cerrar Sesión</a>
        </nav>
    </aside>

    <main class="main-content">

        <header class="topbar">
            <nav class="topbar-nav">
                <a href="{% url 'dashboard' %}">Dashboard</a>
                <a href="#">Catálogos</a>
                <a href="#">Configuración</a>
                <a href="#">Ayuda</a>
            </nav>
            <div class="topbar-right">
                <div class="topbar-search">
                    <i data-feather="search"></i>
                    <input type="text" placeholder="Buscar...">
                </div>
                <div class="topbar-lang">Eng ▾</div>
            </div>
        </header>

        <section class="content">

            <!-- HEADER -->
            <div class="page-header">
                <div class="page-header-text">
                    <h1>Administración de usuarios</h1>
                    <p>Gestiona usuarios del sistema, edita su información y controla su estado.</p>
                </div>

                <div style="display:flex; align-items:center; gap:15px;">
                    <a href="{% url 'usuario_crear' %}" class="btn-primary">
                        + Nuevo Usuario
                    </a>
                    <div class="page-header-logo-min">
                        <img src="{% static 'users/img/logo.png' %}">
                    </div>
                </div>
            </div>

            <!-- CONTROLES -->
            <div class="table-controls">
                <div class="table-controls-left">
                    <form method="get">
                        <input type="text" name="buscar" value="{{ request.GET.buscar }}" placeholder="Buscar usuario...">
                    </form>
                </div>
            </div>

            <!-- TABLA -->
            <table class="modern-table">
                <thead>
                    <tr>
                        <td>Usuario</td>
                        <td>Nombre</td>
                        <td>Email</td>
                        <td>Estado</td>
                        <td>Acciones</td>
                    </tr>
                </thead>
                <tbody>
                {% for user in usuarios %}
                    <tr>
                        <td>{{ user.username }}</td>
                        <td>{{ user.get_full_name }}</td>
                        <td>{{ user.email }}</td>

                        <td>
                            {% if user.is_active %}
                                <span class="badge badge-success">Activo</span>
                            {% else %}
                                <span class="badge badge-danger">Inactivo</span>
                            {% endif %}
                        </td>

                        <td>
                            <div class="actions">
                                <a href="{% url 'usuario_detalle' user.pk %}" class="btn-icon">
                                    <i data-feather="eye"></i>
                                </a>

                                <a href="{% url 'usuario_editar' user.pk %}" class="btn-icon">
                                    <i data-feather="edit"></i>
                                </a>

                                <a href="{% url 'usuario_toggle' user.pk %}" class="btn-icon">
                                    {% if user.is_active %}
                                        <i data-feather="toggle-right"></i>
                                    {% else %}
                                        <i data-feather="toggle-left"></i>
                                    {% endif %}
                                </a>
                            </div>
                        </td>
                    </tr>
                {% empty %}
                    <tr>
                        <td colspan="5">No hay usuarios registrados.</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

            <!-- PAGINACIÓN -->
            <div class="pagination">
                <div>
                    Página {{ page_obj.number }} de {{ page_obj.paginator.num_pages }}
                </div>

                <div class="pagination-controls">
                    {% if page_obj.has_previous %}
                        <a href="?page={{ page_obj.previous_page_number }}">Anterior</a>
                    {% endif %}

                    <span class="current">{{ page_obj.number }}</span>

                    {% if page_obj.has_next %}
                        <a href="?page={{ page_obj.next_page_number }}">Siguiente</a>
                    {% endif %}
                </div>
            </div>

        </section>
    </main>
</div>

<script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>
<script>feather.replace({'stroke-width': 2})</script>
</body>
</html>